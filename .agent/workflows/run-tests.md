---
description: Run application tests
---

1. Execute the build and test lifecycle for the Apps Script repository.
2. If inside the `apps-script/` directory, run TypeScript compilation `npx tsc --noEmit` to verify type safety.
3. Run any generic smoke tests covering script invocation or module validation.
