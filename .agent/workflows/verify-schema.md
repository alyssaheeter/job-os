---
description: Validate JSON schemas for Google Sheets tabs
---

1. Locate the JSON schemas in `schemas/`.
2. Check `SETTINGS.schema.json` to ensure it parses correctly and matches the minimum required keys defined in the project.
3. If mock data is available, validate the schema against a dummy `.json` file containing key/value pairs.
4. Report any missing required keys or schema mismatches.
