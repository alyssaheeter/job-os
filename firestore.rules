rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for any authenticated user (personal tool — single-user)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
